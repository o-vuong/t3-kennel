name: Container Placeholder

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    name: Placeholder — Build container image
    runs-on: ubuntu-latest
    steps:
      - name: TODO docker build in Phase 4
        run: echo "Phase 4: add docker build/push"

  attest:
    name: Placeholder — Cosign attest
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: TODO cosign attest in Phase 4
        run: echo "Phase 4: add cosign attest"
